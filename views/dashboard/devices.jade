table(style='width:100%')
  tr
    th ID
    th Gateway Type
    th Gateway Name
    th location
    th Created On
  
  each value in details
    tr
      th #{value.id} 
      th #{value.metadata.type}
      th #{value.metadata.name}
      th #{value.location}
      th #{value.create_date}
